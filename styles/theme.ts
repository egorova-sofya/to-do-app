import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#161719",
    },
    secondary: {
      main: "#bb77ed",
    },
  },
});

export default theme;
